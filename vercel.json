{
  "buildCommand": "./tasks/download-repos.sh && astro build",
  "installCommand": "dnf -y install jq && npm install",
  "crons": [
    {
      "path": "/api/cron/deploy",
      "schedule": "0 18 * * *"
    }
  ]
}
