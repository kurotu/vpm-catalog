---
layout: ~/layouts/MarkdownLayout.astro
title: VPM Catalog Repository
---
import { site } from '~/../astro.config.mjs';
import { vccAddRepoLink } from '~/utils/vpm';
export const repoURL = `${new URL('/index.json', site || 'http://localhost:4321')}`
export const addToVCCURL = vccAddRepoLink(repoURL);

A VPM repository which contains all the packages listed in the VPM Catalog excepting [Official](/repositories/com_vrchat_repos_official) and [Curated](/repositories/com_vrchat_repos_curated) repositories.

This repository is provided as is, without warranty of any kind.

## Repository Information

<pre>
{repoURL}
</pre>

<a href={addToVCCURL} class="!btn !btn-lg !btn-wide !btn-primary">Add to VCC</a>
